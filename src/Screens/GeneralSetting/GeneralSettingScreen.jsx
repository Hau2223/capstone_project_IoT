import {StyleSheet, Text, View} from 'react-native';
import React, {memo} from 'react';

const GeneralSettingScreen = () => {
  return (
    <View>
      <Text>GeneralSettingScreen</Text>
    </View>
  );
};

export default memo(GeneralSettingScreen);

const styles = StyleSheet.create({});
